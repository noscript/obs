#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	tar xavf $(shell find /usr/src/ -type f -name 'linux-source-*.tar.xz')

override_dh_auto_build:
	cd linux-source-*/
	make ARCH=i386 defconfig
	dh_auto_build -- bindeb-pkg LOCALVERSION=-obs
